# CodeLab AI Service

Микросервисная архитектура для AI агента, предоставляющая унифицированный доступ к различным LLM провайдерам и инструментам для работы с кодом.

**Обновлено:** Все сервисы переведены на слоистую, масштабируемую архитектуру (папки: api, models, services, middleware, core) c современным шаблоном проектирования на Python 3.12+. Тестирование, линтинг и автотесты унифицированы для каждого сервиса.

## 🎯 Статус проекта

**Текущий этап:** Фаза 1 - Базовая инфраструктура
- ✅ Итерация 1.1: Настройка всех сервисов (ЗАВЕРШЕНО)
- 🏗 Итерация 1.2: Базовый поток сообщений (В ПРОЦЕССЕ)

## 🚀 Основные компоненты и схемы интеграции

*Все сервисы масштабируемы и легко расширяются — реализация новых LLM, инструментов, очередей или версий API не требует переписывания других компонентов.*

Актуальные routes и схемы обмена между сервисами:

| Сервис         | REST/SSE endpoint                         | WebSocket endpoint         | Особенности                       |
|----------------|:------------------------------------------|----------------------------|------------------------------------|
| gateway        | /health                                  | /ws/{session_id}           | Через WebSocket проксирует стримовые токены между IDE и agent-runtime |
| agent-runtime  | /health, /agent/message/stream           |                            | Проксирует SSE к llm-proxy на /v1/chat/completions, нужен x-internal-auth |
| llm-proxy      | /health, /v1/llm/models, /v1/chat/completions |                            | SSE, REST endpoint, все требуют x-internal-auth |

**Документация по микросервисам и всем актуальным REST/WebSocket API** — см. внутренний  `README.md` соответствующего сервиса ([gateway/README.md](gateway/README.md), [agent-runtime/README.md](agent-runtime/README.md), [llm-proxy/README.md](llm-proxy/README.md)).


### Gateway Service
- WebSocket прокси между IDE и Agent
- Управление сессиями
- Маршрутизация сообщений

### Agent Runtime Service
- Основная AI логика
- Управление контекстом
- Обработка инструментов

### LLM Proxy Service
- Унифицированный доступ к LLM провайдерам
- Поддержка OpenAI и Anthropic
- Потоковая передача ответов

## 📋 Требования

- Python 3.12+ (рекомендуется, во всех сервисах протестирована совместимость именно с этой версией)
- Docker и Docker Compose
- uv (быстрый Python package installer)

## 🔑 Внутренняя авторизация между микросервисами

Все внутренние REST/SSE-запросы защищены общим ключом `INTERNAL_API_KEY`:
- Заголовок X-Internal-Auth: my-super-secret-key (либо ваш ключ из .env) прописан во всех тестах и curl
- Тот же ключ должен быть в каждом сервисе (.env/.env.example); пробрасывается через docker-compose
- Без правильного ключа все защищённые endpoint'ы вернут 401 Unauthorized

Пример curl (SSE):
```bash
curl -X POST http://localhost:8001/agent/message/stream \
    -H "X-Internal-Auth: my-super-secret-key" \
    -H "Content-Type: application/json" \
    -d '{"session_id": "demo", "type": "user_message", "content": "Привет!"}'
```

Запрос к LLM Proxy новым API:
```bash
curl -X POST http://localhost:8002/v1/chat/completions \
    -H "X-Internal-Auth: my-super-secret-key" \
    -H "Content-Type: application/json" \
    -d '{"model": "gpt-4", "messages": [{"role": "user", "content": "Say hello!"}], "stream": true, "temperature": 1}'
```

## 🛠 Установка

<details>
<summary><b>Архитектурные слои в каждом сервисе</b></summary>

- <b>app/api/</b> — entrypoints FastAPI (REST/WebSocket)
- <b>app/models/</b> — Pydantic-схемы для всех API/внутренних структур
- <b>app/services/</b> — бизнес-логика (LLM, буферы, сторы, инструменты)
- <b>app/middleware/</b> — авторизация, логирование
- <b>app/core/</b> — переменные окружения, DI, логгер, базовые компоненты
</details>

1. Клонируйте репозиторий:
```bash
git clone https://github.com/openidealab/codelab-ai-service.git
cd codelab-ai-service
```

2. Установите uv:
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

3. Создайте файл .env на основе .env.example:
```bash
cp .env.example .env
```

4. Настройте переменные окружения в .env файле:
- Добавьте ваш OPENAI_API_KEY
- Добавьте ваш ANTHROPIC_API_KEY
- При необходимости измените порты и другие параметры

5. Запустите сервисы:
```bash
docker compose up -d
```

## 🔍 Проверка работоспособности

Проверьте статус сервисов (health):
```bash
curl http://localhost:8000/health  # gateway
curl http://localhost:8001/health  # agent-runtime
curl http://localhost:8002/health  # llm-proxy
```

## 🔌 Примеры работы с актуальными endpoint'ами

### Получить список LLM моделей
```bash
curl -X GET http://localhost:8002/v1/llm/models \
  -H "X-Internal-Auth: my-super-secret-key"
```

### Потоковое общение через agent-runtime (SSE)
```bash
curl -X POST http://localhost:8001/agent/message/stream \
  -H "X-Internal-Auth: my-super-secret-key" \
  -H "Content-Type: application/json" \
  -d '{"session_id": "demo", "type": "user_message", "content": "Тест!"}'
```

### Прямой запрос к LLM-proxy (SSE, token-by-token, совместимо с OpenAI)
```bash
curl -X POST http://localhost:8002/v1/chat/completions \
  -H "X-Internal-Auth: my-super-secret-key" \
  -H "Content-Type: application/json" \
  -d '{"model": "gpt-4", "messages": [{"role": "user", "content": "Say hello!"}], "stream": true, "temperature": 1}'
```

Формат SSE-ответа:
```
data: { ... }
data: { ... }
data: [DONE]
```

---

## 🔌 WebSocket API

Подключение к WebSocket:
```javascript
const ws = new WebSocket('ws://localhost:8000/ws/{session_id}');

// Отправка сообщения
ws.send(JSON.stringify({
    type: "user_message",
    content: "Your message here"
}));

// Получение ответа
ws.onmessage = (event) => {
    const response = JSON.parse(event.data);
    console.log(response);
};
```

## 🛠 Локальная разработка

### Использование тестов и code style

- Каждый сервис содержит свои юнит- и e2e-тесты в папке `tests/`, структурированные по слоям приложения.
- Для проверки автотестов используйте:
  ```bash
  cd llm-proxy && uv run pytest tests
  cd ../agent-runtime && uv run pytest tests
  cd ../gateway && uv run pytest tests
  ```
- Для интеграционных/сквозных тестов обязательно запустите все сервисы через `docker compose up -d`.
- Весь код отформатирован автоформаттером Ruff, для проверки запустите:
  ```bash
  ruff check app/
  ruff check app/ --fix
  ```
- Для обновления зависимостей во всех сервисах используйте `uv`, а dev-инструменты (`ruff`, `ty`) входят в `[dev]` секцию pyproject.toml каждого микросервиса.


Для каждого микросервиса (gateway, agent-runtime, llm-proxy) рекомендуется использовать изолированное окружение и одну и ту же версию Python (например, 3.12 через pyenv) для корректной работы всех зависимостей.

### Быстрая инструкция:

1. **Перейдите в папку сервиса:**
   ```bash
   cd gateway            # или agent-runtime, или llm-proxy
   ```
2. **Выберите нужную версию Python (через pyenv):**
   ```bash
   pyenv local 3.12.2    # только если установлен pyenv и python 3.12.2
   ```
3. **Создайте и активируйте виртуальное окружение:**
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   ```
4. **Проверьте версию Python:**
   ```bash
   python --version      # должно быть Python 3.12.x
   ```
5. **Установите зависимости с помощью uv:**
   ```bash
   uv pip install -e . --upgrade
   ```
6. **Запустите сервис:**
   ```bash
   python app/main.py
   ```

**Повторите шаги для каждого сервиса.**

> 💡 Если pyenv не используется — просто убедитесь, что выбранная версия Python совместима с pydantic (3.12.x или ниже)



## 🔧 Управление зависимостями

*Настоятельно рекомендуется держать версию Python одинаковой во всех сервисах для полной совместимости!*

Проект использует современный менеджер пакетов uv и pyproject.toml для управления зависимостями:

```bash
# Установка зависимостей
uv pip install -e .

# Обновление зависимостей
uv pip install -e . --upgrade
```

## 🧰 Инструменты разработки: Ruff и Ty

- **[Ruff](https://docs.astral.sh/ruff/):** быстрый python-линтер и автоформаттер.
  - Проверить стиль и ошибки: `ruff check app/`
  - Автоматически исправить: `ruff check app/ --fix`
  - Настройки — через pyproject.toml; работает «из коробки» с VSCode (рекомендуется установить расширение Ruff).

- **[Ty](https://docs.astral.sh/ty/):** удобный CLI-инструмент для работы с зависимостями.
  - Конвертация requirements.txt → pyproject.toml:  
    `ty requirements.txt pyproject.toml`
  - Экспорт зависимостей в requirements.txt:  
    `ty pyproject.toml > requirements.txt`

**Оба инструмента входят в dev-зависимости сервиса. Для их установки:**
```bash
uv pip install -e '.[dev]'
```
или
```bash
pip install -e '.[dev]'
```

**В VSCode:**  
- Ruff — работает с расширением “Ruff” и из командной строки.  
- Ty — использовать в терминале или добавить в задачи (`.vscode/tasks.json`).

Подробнее: [Документация Ruff](https://docs.astral.sh/ruff/) и [Документация Ty](https://docs.astral.sh/ty/)


---

## 🧩 Масштабируемость, развитие и документация

- Каждый сервис легко расширяется новым функционалом за счёт чётких слоёв (api, models, services, middleware, core).
- Для добавления новых версий API достаточно создать новый router в папке `api/v2` и подключить его в main.py.
- Технологии легко обновляются: простая миграция на новые модели, внешние provider'ы, системы хранения и авторизации.
- См. дополнительную документацию в папке `doc/`, а также внутренние README каждого микросервиса.

## ⚙️ Пример настройки VSCode (.vscode/settings.json)

Рекомендуется создать в каждом сервисе файл `.vscode/settings.json` со следующим содержимым:

```json
{
    // Python settings
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
    "python.languageServer": "None",
    
    // Ty settings (основной language server)
    "ty.disableLanguageServices": false,
    "ty.diagnosticMode": "workspace",
    "ty.inlayHints.variableTypes": true,
    "ty.inlayHints.callArgumentNames": true,
    "ty.experimental.rename": true,
    "ty.importStrategy": "useBundled",
    
    // Ruff settings (форматирование и линтинг)
    "[python]": {
        "editor.defaultFormatter": "charliermarsh.ruff",
        "editor.formatOnSave": true,
        "editor.codeActionsOnSave": {
            "source.fixAll": "explicit"
        }
    },
    
    // Editor settings
    "editor.formatOnSave": true,
    "files.exclude": {
        "**/__pycache__": true,
        "**/.pytest_cache": true,
        "**/.ruff_cache": true
    }
}
```

workspace:

```json
{
	"folders": [
		{
			"path": "."
		},
		{
			"path": "llm-proxy"
		},
		{
			"path": "gateway"
		},
		{
			"path": "agent-runtime"
		}
	],
	"settings": {
		"python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
		"python.languageServer": "None",
		"ty.disableLanguageServices": false,
		"ty.diagnosticMode": "workspace",
		"ty.inlayHints.variableTypes": true,
		"ty.inlayHints.callArgumentNames": true,
		"ty.experimental.rename": true,
		"ty.importStrategy": "useBundled",
		"[python]": {
			"editor.defaultFormatter": "charliermarsh.ruff",
			"editor.formatOnSave": true,
			"editor.codeActionsOnSave": {
				"source.fixAll": "explicit"
			}
		},
		"editor.formatOnSave": true,
		"files.exclude": {
			"**/__pycache__": true,
			"**/.pytest_cache": true,
			"**/.ruff_cache": true
		},
		"python.testing.pytestArgs": [
			"."
		],
		"python.testing.unittestEnabled": false,
		"python.testing.pytestEnabled": true
	}
}
```

> 💡 Этот конфиг настраивает:
> - Ty как основной language server для Python
> - Ruff для форматирования и линтинга
> - Автоматическое форматирование при сохранении
> - Исключение временных файлов из отображения

## 📝 Лицензия

MIT License - см. [LICENSE](LICENSE) файл для деталей.

## 🤝 Участие в разработке

1. Fork репозитория
2. Создайте ветку для ваших изменений
3. Внесите изменения
4. Отправьте Pull Request

## 📚 Документация

Подробная документация доступна в директории `doc/`:
- [План разработки](doc/ai-agent-iterative-development-plan.md)
- [Требования к Gateway](doc/tech-req-gateway.md)
- [Требования к Agent Runtime](doc/tech-req-agent-runtime-service.md)
- [Требования к LLM Proxy](doc/tech-req-llm-proxy-service.md)
