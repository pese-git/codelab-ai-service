# URL for the agent service
GATEWAY__AGENT_URL=http://localhost:8001

# Internal API key for authentication between services
GATEWAY__INTERNAL_API_KEY=change-me-internal-key

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
GATEWAY__LOG_LEVEL=DEBUG

# Request timeout in seconds
GATEWAY__REQUEST_TIMEOUT=30.0

# Service version
GATEWAY__VERSION=0.1.0