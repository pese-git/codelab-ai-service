# CHANGELOG: Исправление множественных вызовов инструментов

## [2025-12-29] - Критическое исправление

### Проблема
AI агент пытался вызывать несколько инструментов одновременно, не дожидаясь завершения предыдущих операций. Это приводило к попыткам чтения файлов до их создания (например, при `flutter create`).

### Изменения

#### 1. Системный промпт (`app/core/agent/prompts.py`)
**Добавлено:**
- Секция "CRITICAL: Tool Usage Rules" с явными инструкциями
- Правило: использовать только ОДИН инструмент за раз
- Правило: ждать результата перед следующим вызовом
- Правило: работать итеративно (tool → result → analyze → next tool)
- Конкретный пример для `flutter create`

**Эффект:**
- LLM теперь явно проинструктирован работать последовательно
- Снижение вероятности множественных tool calls

#### 2. Валидация в коде (`app/services/llm_stream_service.py`)
**Добавлено:**
- Проверка `len(tool_calls) > 1` перед выполнением
- Логирование предупреждений при нарушении правила
- Информирование о том, какие инструменты были проигнорированы

**Эффект:**
- Раннее обнаружение проблемы
- Детальное логирование для отладки
- Защита от некорректного поведения

### Технические детали

**Файлы изменены:**
- `app/core/agent/prompts.py` - строки 133-143
- `app/services/llm_stream_service.py` - строки 112-122

**Обратная совместимость:**
- ✅ Полная обратная совместимость
- ✅ Не требует изменений в клиентском коде
- ✅ Не меняет API

### Тестирование

**Сценарий тестирования:**
1. Запросить создание Flutter проекта
2. Проверить логи на отсутствие предупреждений о множественных tool calls
3. Убедиться в последовательном выполнении: `execute_command` → результат → `list_files` → результат → `read_file`

**Ожидаемое поведение:**
- Агент вызывает `execute_command("flutter create")`
- Ждет завершения команды
- Только после получения результата вызывает `list_files` или `read_file`
- Никогда не пытается читать несуществующие файлы

### Связанная документация

См. подробное описание в [`doc/agent-tool-usage-fix.md`](../../doc/agent-tool-usage-fix.md)

### Приоритет

🔴 **КРИТИЧЕСКИЙ** - Исправляет фундаментальную проблему в работе агента

### Автор

Исправление от 2025-12-29
