# Анализ проблемы зацикливания

**Дата:** 19 января 2026  
**Проблема:** Бесконечные вызовы tools после смены агента

---

## 🔍 Анализ

### Симптомы
- После открытия новой сессии и смены агента
- Начинаются бесконечные вызовы tools
- Система зацикливается

### Возможные причины

#### 1. Двойная обработка в MessageOrchestrationService ❌ (ИСКЛЮЧЕНО)

**Проверка логики:**
- Строки 170-226: Orchestrator выполняет маршрутизацию
- Строки 228-293: Обработка через текущего агента

**Вывод:** Логика идентична старому MultiAgentOrchestrator (строки 136-252).
Это правильное поведение - после маршрутизации Orchestrator, `context.current_agent`
уже обновлен на нового агента, поэтому строка 229 получает правильного агента.

#### 2. Используется старый endpoint ⚠️ (ВЕРОЯТНО)

**Проверка:**
- Старый endpoint: `/api/v1/agent/stream` (в endpoints.py)
- Новый endpoint: `/agent/message/stream` (в messages_router.py)

**Вопрос:** Какой endpoint использует Gateway?

#### 3. Проблема в самом агенте ⚠️ (ВОЗМОЖНО)

Агент может зацикливаться при обработке tool results.

#### 4. Проблема с tool_result обработкой ⚠️ (ВОЗМОЖНО)

В новом messages_router.py tool_result пока не реализован (строки 119-145).

---

## 🔧 Рекомендации по диагностике

### 1. Проверить какой endpoint используется

```bash
# В логах сервиса найти:
grep "Processing user message" logs.txt

# Если видите:
# "via MessageOrchestrationService" - используется новый endpoint
# Если нет этого текста - используется старый endpoint
```

### 2. Проверить логи агента

```bash
# Найти в логах:
grep "Processing with.*agent" logs.txt

# Проверить:
# - Какой агент обрабатывает сообщение
# - Сколько раз вызывается один и тот же агент
# - Есть ли переключения агентов
```

### 3. Проверить tool calls

```bash
# Найти в логах:
grep "tool_call" logs.txt

# Проверить:
# - Какие tools вызываются
# - Повторяются ли одни и те же tool calls
# - Есть ли tool_result в ответах
```

---

## 🛠️ Возможные решения

### Решение 1: Отключить новый endpoint (временно)

Если проблема в новом коде, можно временно отключить messages_router:

**Файл:** `app/main.py`

```python
# Закомментировать:
# from app.api.v1.routers import (
#     ...
#     messages_router  # <-- закомментировать
# )

# И:
# app.include_router(messages_router)  # <-- закомментировать
```

### Решение 2: Добавить защиту от зацикливания

Добавить счетчик итераций в MessageOrchestrationService:

```python
MAX_ITERATIONS = 10
iteration = 0

async for chunk in current_agent.process(...):
    iteration += 1
    if iteration > MAX_ITERATIONS:
        logger.error(f"Too many iterations ({iteration}), breaking loop")
        yield StreamChunk(type="error", error="Too many iterations")
        return
    
    yield chunk
```

### Решение 3: Проверить логику tool_result

Убедиться, что tool_result правильно обрабатывается и не вызывает повторную обработку.

---

## 📋 Чеклист диагностики

- [ ] Проверить какой endpoint используется (старый или новый)
- [ ] Проверить логи агента (какой агент, сколько раз)
- [ ] Проверить tool calls (какие tools, повторяются ли)
- [ ] Проверить tool_result обработку
- [ ] Попробовать отключить новый endpoint
- [ ] Добавить защиту от зацикливания
- [ ] Проверить логику переключения агентов

---

## 🎯 Следующие действия

1. **Собрать логи:** Получить полные логи с момента зацикливания
2. **Определить endpoint:** Какой endpoint используется
3. **Анализ:** Понять где именно происходит зацикливание
4. **Исправление:** Применить соответствующее решение

---

**Автор:** AI Assistant  
**Дата:** 19 января 2026  
**Статус:** Требуется дополнительная диагностика
