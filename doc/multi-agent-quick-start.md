# Быстрый старт: Мультиагентная система

**Версия:** 1.0.0
**Дата:** 20 января 2026
**Статус:** ✅ Реализовано

---

## Обзор

Мультиагентная система с 5 специализированными агентами:

| Агент | Роль | Основные возможности |
|-------|------|---------------------|
| **Orchestrator** | Координатор | Анализирует запросы, определяет нужного агента |
| **Coder** | Разработчик | Пишет/модифицирует код, создает файлы |
| **Architect** | Архитектор | Проектирует системы, создает спецификации (.md) |
| **Debug** | Отладчик | Исследует ошибки, находит причины багов |
| **Ask** | Консультант | Отвечает на вопросы, объясняет концепции |

## Примеры использования

### 1. Автоматическое определение агента

```bash
curl -X POST http://localhost:8001/agent/message/stream \
  -H "Content-Type: application/json" \
  -H "x-internal-auth: your-key" \
  -d '{
    "session_id": "session_123",
    "message": {
      "type": "user_message",
      "content": "Create a new Flutter widget for user profile"
    }
  }'
```

**Результат:** Orchestrator → Coder (автоматически)

### 2. Явное переключение агента

```bash
curl -X POST http://localhost:8001/agent/message/stream \
  -H "Content-Type: application/json" \
  -H "x-internal-auth: your-key" \
  -d '{
    "session_id": "session_123",
    "message": {
      "type": "switch_agent",
      "agent_type": "architect",
      "content": "Design the architecture for authentication"
    }
  }'
```

## Сценарии работы

### Сценарий 1: Создание компонента

```
User: "Create a new user profile widget"
  ↓
Orchestrator → Coder
  ↓
Coder:
  1. list_files("lib/widgets")
  2. write_file("lib/widgets/user_profile.dart", content)
  3. attempt_completion("Created widget")
```

### Сценарий 2: Отладка ошибки

```
User: "Why am I getting null pointer exception?"
  ↓
Orchestrator → Debug
  ↓
Debug:
  1. read_file("lib/main.dart")
  2. search_in_code("null")
  3. Анализ → находит причину
  4. attempt_completion("Found issue...")
  ↓
User: "Fix it"
  ↓
Debug → Coder
  ↓
Coder: Исправляет код
```

### Сценарий 3: Проектирование

```
User: "Design authentication system"
  ↓
Orchestrator → Architect
  ↓
Architect:
  1. list_files(".")
  2. write_file("docs/auth-design.md", spec)
  3. attempt_completion("Created design")
  ↓
User: "Implement login screen"
  ↓
Architect → Coder
  ↓
Coder: Реализует по спецификации
```

## Структура файлов для реализации

```
app/
├── agents/
│   ├── __init__.py                 # Инициализация агентов
│   ├── base_agent.py               # Базовый класс
│   ├── orchestrator_agent.py       # Orchestrator
│   ├── coder_agent.py              # Coder
│   ├── architect_agent.py          # Architect
│   ├── debug_agent.py              # Debug
│   ├── ask_agent.py                # Ask
│   └── prompts/
│       ├── orchestrator.py
│       ├── coder.py
│       ├── architect.py
│       ├── debug.py
│       └── ask.py
├── services/
│   ├── agent_router.py             # Маршрутизация
│   ├── agent_context.py            # Контекст агента
│   └── multi_agent_orchestrator.py # Оркестрация
└── models/
    └── agent_schemas.py            # Схемы данных
```

## Ограничения агентов

### Coder
- ✅ Все инструменты для работы с кодом
- ✅ Может редактировать любые файлы
- ✅ Может выполнять команды

### Architect
- ✅ Только чтение кода
- ⚠️ Может редактировать только .md файлы
- ❌ Не может выполнять команды

### Debug
- ✅ Чтение файлов и поиск
- ✅ Выполнение команд (тесты, логи)
- ❌ Не может редактировать файлы

### Ask
- ✅ Только чтение файлов
- ❌ Не может редактировать
- ❌ Не может выполнять команды

## Конфигурация

```bash
# .env
AGENT_RUNTIME__MULTI_AGENT_ENABLED=true
AGENT_RUNTIME__DEFAULT_AGENT=orchestrator
AGENT_RUNTIME__AUTO_AGENT_SWITCHING=true
AGENT_RUNTIME__MAX_AGENT_SWITCHES=10
```

## Следующие шаги

1. Изучите [полный план архитектуры](./multi-agent-architecture-plan.md)
2. Посмотрите [диаграммы](./multi-agent-architecture-diagram.md)
3. Начните реализацию с базовых классов
4. Добавьте специализированных агентов
5. Интегрируйте с API endpoints

## Ключевые преимущества

- 🎯 **Специализация** - каждый агент эксперт в своей области
- 🔒 **Безопасность** - ограничения на инструменты и файлы
- 🔄 **Гибкость** - автоматическое и ручное переключение
- 📊 **Наблюдаемость** - полное логирование действий
- 🚀 **Расширяемость** - легко добавлять новых агентов
