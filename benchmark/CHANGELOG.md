# Benchmark Suite Changelog

## 2026-01-13 - v2.0

### ✨ Major Features

#### 1. Расширение benchmark suite (10 → 40 задач)
- Добавлено 30 новых задач для полноценного POC тестирования
- Распределение по категориям:
  - Простые (A): 10 задач
  - Средние (B): 15 задач
  - Сложные (C): 10 задач
  - Специализированные (D): 5 задач
- Распределение по типам:
  - Coding: 16 задач (40%)
  - Architecture: 8 задач (20%)
  - Debug: 8 задач (20%)
  - Question: 4 задачи (10%)
  - Mixed: 4 задачи (10%)

#### 2. Реорганизация в отдельную директорию
- Создана директория `benchmark/` для всех связанных файлов
- Перемещены: задачи, спецификации, метрики, скрипты
- Создана поддиректория `reports/` для отчетов
- Обновлены все пути и импорты

#### 3. Два режима работы

**Режим симуляции** (`run_poc_experiment.py`):
- Для тестирования инфраструктуры метрик
- Не требует LLM proxy
- Быстрое выполнение (секунды)
- Генерирует случайные метрики

**Режим реальной интеграции** (`run_poc_experiment_integrated.py`):
- Интеграция с `multi_agent_orchestrator`
- Реальное выполнение через агентов
- Сбор реальных метрик из `StreamChunk`
- Требует запущенный LLM proxy

#### 4. Гибкая фильтрация задач

Добавлены опции для обоих скриптов:
- `--task-id` - одна задача
- `--task-ids` - список задач через запятую
- `--task-range` - диапазон задач (e.g., 1-10)
- `--category` - фильтр по категории
- `--type` - фильтр по типу
- `--limit` - ограничение количества

### 🐛 Bug Fixes

#### Улучшен парсинг JSON в orchestrator
- Добавлен regex fallback для извлечения JSON
- Улучшена обработка markdown code blocks
- Fallback на keyword-based classification при ошибках

### 📚 Документация

- `README.md` - обновлен с двумя режимами и примерами фильтрации
- `INTEGRATION_GUIDE.md` - подробное руководство по интеграции
- `CHANGELOG.md` - этот файл

### 🧪 Тестирование

**Симуляция:**
- ✅ 40 задач выполнено за 0.5 секунды
- ✅ Success rate: 70%
- ✅ Все фильтры протестированы

**Реальная интеграция:**
- ✅ Интеграция с orchestrator работает
- ✅ Agent switches записываются
- ✅ Метрики собираются
- ✅ Все фильтры протестированы

### 📦 Git Commits

1. `9324994` - Расширение до 40 задач и реорганизация
2. `24209ba` - Добавление реальной интеграции
3. `fdad034` - Гибкая фильтрация задач
4. `be646a6` - Обновление документации

## Примеры использования

### Быстрое тестирование (симуляция)
```bash
# Все задачи
uv run python ../benchmark/scripts/run_poc_experiment.py --mode both

# Только простые задачи
uv run python ../benchmark/scripts/run_poc_experiment.py --mode single-agent --category simple

# Первые 10 задач
uv run python ../benchmark/scripts/run_poc_experiment.py --mode single-agent --task-range 1-10
```

### Реальный POC (требует LLM proxy)
```bash
# Одна задача для теста
uv run python ../benchmark/scripts/run_poc_experiment_integrated.py --mode multi-agent --task-id task_001

# Все coding задачи
uv run python ../benchmark/scripts/run_poc_experiment_integrated.py --mode multi-agent --type coding

# Полный эксперимент
uv run python ../benchmark/scripts/run_poc_experiment_integrated.py --mode both
```

### Генерация отчета
```bash
uv run python ../benchmark/scripts/generate_metrics_report.py --latest --output ../benchmark/reports/poc_report.md
```

## Следующие шаги

1. ✅ Инфраструктура метрик готова
2. ✅ 40 задач созданы
3. ✅ Два режима работы реализованы
4. ✅ Гибкая фильтрация добавлена
5. ⏳ Настроить LLM API ключи для полного POC
6. ⏳ Запустить полный эксперимент на всех 40 задачах
7. ⏳ Сравнить результаты single-agent vs multi-agent
